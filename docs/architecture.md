# HypeProof Lab
## AI Agent Orchestration Architecture Reference Document

**Version 1.0 | January 2025**

---

## 목차 (Table of Contents)

1. [Executive Summary](#1-executive-summary)
2. [핵심 개념 (Core Concepts)](#2-핵심-개념-core-concepts)
3. [아키텍처 구조 (Architecture Structure)](#3-아키텍처-구조-architecture-structure)
4. [컴포넌트 정의 (Component Definitions)](#4-컴포넌트-정의-component-definitions)
5. [디자인 패턴 (Design Patterns)](#5-디자인-패턴-design-patterns)
6. [SDK 구현 가이드 (SDK Implementation Guide)](#6-sdk-구현-가이드-sdk-implementation-guide)
7. [확장 전략 (Scaling Strategy)](#7-확장-전략-scaling-strategy)
8. [로드맵 (Roadmap)](#8-로드맵-roadmap)
9. [Appendix: 용어집 (Glossary)](#appendix-용어집-glossary)

---

## 1. Executive Summary

본 문서는 HypeProof Lab의 AI 오케스트레이션 아키텍처를 정의합니다. 이 아키텍처는 Claude Code SDK를 기반으로 확장 가능한 멀티 에이전트 시스템을 구축하기 위한 청사진을 제공합니다.

### 1.1 핵심 원칙

- **시간 효율성 우선**: 토큰 비용보다 실행 시간 최적화
- **병렬 처리**: 독립적인 작업은 동시에 실행
- **컨텍스트 격리**: 각 에이전트는 독립 컨텍스트 윈도우 보유
- **조합 가능성**: 스킬과 에이전트의 조합으로 새 워크플로우 생성
- **스펙 기반 개발**: TDD/DDD 원칙에 따른 명확한 인터페이스 정의

### 1.2 기대 효과

| 측면 | 기존 방식 | 오케스트레이션 |
|------|----------|---------------|
| 작업 처리 | 순차적 (35분) | 병렬 (15분) |
| 컨텍스트 관리 | 단일 (오염 발생) | 격리 (깨끗함) |
| 확장성 | 선형 증가 | 기하급수적 조합 |

---

## 2. 핵심 개념 (Core Concepts)

### 2.1 레스토랑 비유 (Restaurant Analogy)

본 아키텍처는 전문 레스토랑 주방의 운영 방식을 모델로 합니다:

| 레스토랑 | AI 컴포넌트 | 역할 | 특징 |
|---------|------------|------|------|
| 🖥️ 키오스크 | Slash Command | 사용자 진입점 | 명시적 호출 |
| 👨‍🍳 헤드 셰프 | Skill | 오케스트레이터 | 스펙/레시피 보유 |
| 👨‍🍳 라인 쿡 | Sub-Agent | 전문 실행자 | 독립 컨텍스트 |
| 🍳 조리대 | Context Window | 작업 공간 | 격리된 메모리 |
| 📋 레시피 | Spec/Config | 품질 기준 | 재사용 가능 |

### 2.2 계층 구조 (Layered Architecture)

아키텍처는 세 개의 명확한 계층으로 구성됩니다:

| 계층 | 컴포넌트 | 트리거 방식 | 컨텍스트 |
|------|---------|------------|---------|
| Interface Layer | Slash Command | 사용자 명시적 호출 (`/cmd`) | 메인 컨텍스트 |
| Orchestration Layer | Skill | 자동 발견 또는 명시적 | 메인 컨텍스트 |
| Execution Layer | Sub-Agent | Skill에 의한 위임 | 격리 컨텍스트 |

---

## 3. 아키텍처 구조 (Architecture Structure)

### 3.1 기본 흐름도

사용자 요청이 처리되는 기본 흐름:

```
[User] → /command → [Skill] → [Sub-Agent Pool] → [Result]
```

### 3.2 상세 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    📱 Slash Command Layer (진입점)                       │
├─────────────┬─────────────┬─────────────┬─────────────┬────────────────┤
│  /podcast   │  /proposal  │  /research  │ /newsletter │    /신규...    │
└──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┴───────┬────────┘
       │             │             │             │              │
       └─────────────┴──────┬──────┴─────────────┴──────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    👨‍🍳 Skill Layer (오케스트레이션)                        │
├─────────────┬─────────────┬─────────────┬─────────────┬────────────────┤
│ 콘텐츠 제작  │  딥리서치   │  문서 작성   │  품질 검증   │  번역/현지화   │
│   Skill     │   Skill     │   Skill     │   Skill     │    Skill      │
└──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┴───────┬────────┘
       │             │             │             │              │
       └─────────────┴──────┬──────┴─────────────┴──────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    🤖 Sub-Agent Pool (병렬 실행)                         │
├────────┬────────┬────────┬────────┬────────┬────────┬──────────────────┤
│  웹검색 │  코드  │  분석  │  작성  │ 이미지  │  검토  │   데이터처리    │
│ Agent  │ Agent  │ Agent  │ Agent  │ Agent  │ Agent  │     Agent       │
├────────┴────────┴────────┴────────┴────────┴────────┴──────────────────┤
│                      [각자 독립 컨텍스트 윈도우]                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.3 실행 흐름 예시: `/podcast` 커맨드

```
손님: /podcast "Physical AI 트렌드"
                    │
                    ▼
        ┌───────────────────────┐
        │ 👨‍🍳 헤드셰프 (Skill)    │
        │                       │
        │ "오늘 코스 나간다!"    │
        │                       │
        │ 📋 레시피 확인:        │
        │ - 전채: 트렌드 리서치  │
        │ - 메인: 심층 분석      │
        │ - 디저트: 스크립트     │
        └───────────┬───────────┘
                    │
    ┌───────────────┼───────────────┐
    │               │               │
    ▼               ▼               ▼
┌───────┐      ┌───────┐      ┌───────┐
│🥗전채  │      │🥩메인  │      │🍰디저트│
│       │      │       │      │       │
│Research│      │Analysis│      │Writing│
│Agent  │      │Agent  │      │Agent  │
│       │      │       │      │       │
│"웹검색"│      │"데이터" │      │"초안"  │
│"논문"  │      │"구조화" │      │"편집"  │
└───┬───┘      └───┬───┘      └───┬───┘
    │              │              │
    │   ⚡ 동시 조리 (10분)        │
    │              │              │
    └──────────────┼──────────────┘
                   │
                   ▼
        ┌───────────────────────┐
        │ 👨‍🍳 헤드셰프 플레이팅   │
        │                       │
        │ 결과물 통합 + 검수     │
        │                       │
        │ 🍽️ "팟캐스트 완성!"   │
        └───────────────────────┘
```

---

## 4. 컴포넌트 정의 (Component Definitions)

### 4.1 Slash Command

사용자가 명시적으로 호출하는 워크플로우 진입점입니다.

| 속성 | 설명 |
|------|------|
| **트리거** | `/command` 형식으로 사용자가 직접 호출 |
| **역할** | 워크플로우 시작점, 파라미터 검증, Skill 호출 |
| **컨텍스트** | 메인 컨텍스트에서 실행 (프롬프트 인젝션) |
| **위치** | `.claude/commands/` 또는 `~/.claude/commands/` |
| **비유** | 키오스크 메뉴 버튼 (손님이 터치하는 인터페이스) |

**예시 구조:**
```markdown
---
description: Physical AI 팟캐스트 제작
allowed-tools: Task, WebSearch, Read, Write
---

# Podcast: $ARGUMENTS

주제: $ARGUMENTS에 대한 팟캐스트를 제작합니다.

## 실행 순서
1. 콘텐츠 제작 Skill 호출
2. 결과물 검토 및 저장
```

### 4.2 Skill

워크플로우를 정의하고 Sub-Agent들을 조율하는 오케스트레이터입니다.

| 속성 | 설명 |
|------|------|
| **트리거** | 자동 발견 (description 매칭) 또는 명시적 호출 |
| **역할** | 스펙 정의, 작업 분배, 결과 통합, 품질 기준 관리 |
| **컨텍스트** | 메인 컨텍스트에 지식 추가 (경량) |
| **위치** | `.claude/skills/{domain}/SKILL.md` |
| **비유** | 헤드 셰프 (레시피 보유, 라인 쿡들에게 지시) |

**예시 구조:**
```markdown
---
name: content-creation
description: 콘텐츠 제작 워크플로우 오케스트레이션
---

# 콘텐츠 제작 Skill

## 역할
- 리서치, 분석, 작성 에이전트 조율
- 품질 기준 적용
- 결과물 통합

## 호출할 Sub-Agents
- research-agent: 웹 검색 및 자료 수집
- analysis-agent: 데이터 분석 및 구조화
- writing-agent: 초안 작성 및 편집

## 품질 기준
- 정확성: 출처 명시 필수
- 가독성: 문단 당 3-5문장
- 완성도: 서론-본론-결론 구조
```

### 4.3 Sub-Agent

독립된 컨텍스트에서 전문 작업을 수행하는 실행자입니다.

| 속성 | 설명 |
|------|------|
| **트리거** | Skill에 의한 위임 또는 `@agent` 명시적 호출 |
| **역할** | 전문 영역 작업 실행, 독립적 추론, 결과 반환 |
| **컨텍스트** | 독립 컨텍스트 윈도우 (메인 오염 방지) |
| **위치** | `.claude/agents/{agent-name}.md` |
| **비유** | 라인 쿡 (자기 조리대에서 맡은 파트만 집중) |

**예시 구조:**
```markdown
---
name: research-agent
description: 웹 검색 및 자료 수집 전문 에이전트
tools: WebSearch, WebFetch, Read
---

# Research Agent

## 역할
웹 검색을 통해 주제에 대한 최신 정보를 수집합니다.

## 작업 범위
- 공식 문서 및 논문 검색
- 최신 뉴스 및 트렌드 파악
- 신뢰할 수 있는 출처 우선

## 출력 형식
- 주요 발견 사항 요약
- 출처 URL 목록
- 추가 조사 필요 영역 표시
```

---

## 5. 디자인 패턴 (Design Patterns)

### 5.1 병렬 실행 패턴 (Parallel Execution)

독립적인 작업을 동시에 실행하여 시간을 단축합니다.

**적용 사례:**
- 리서치, 분석, 초안 작성을 동시에 진행
- 여러 소스에서 동시에 정보 수집

**장점:**
- 순차 대비 60% 시간 절약
- 독립 작업의 효율적 처리

**주의사항:**
- 쓰기 작업은 단일 스레드로 유지 (충돌 방지)
- 결과 통합 단계 필요

```
기존 (순차):  리서치 → 분석 → 작성 → 검토 = 35분
                10분    10분   10분   5분

병렬 구조:    리서치 ─┐
              분석   ─┼─→ 통합 검토 = 15분
              작성   ─┘
               10분       5분
```

### 5.2 컨텍스트 격리 패턴 (Context Isolation)

각 Sub-Agent가 독립 컨텍스트에서 작업하여 오염을 방지합니다.

**적용 사례:**
- 대용량 파일 분석
- 복잡한 코드베이스 탐색
- 긴 문서 처리

**장점:**
- 토큰 67% 절약 (LangChain 벤치마크)
- 메인 컨텍스트 깨끗하게 유지
- 각 에이전트의 집중력 향상

**연구 근거:**
> "Subagents processes 67% fewer tokens overall due to context isolation" - LangChain Multi-Agent Documentation

### 5.3 스펙 기반 패턴 (Spec-Driven)

Skill이 명확한 스펙/계약을 정의하고 Sub-Agent가 이를 구현합니다.

**적용 사례:**
- TDD (Test-Driven Development)
- DDD (Domain-Driven Design)
- Spec-Driven Development

**구조:**
```
Skill = 인터페이스/스펙 (What to do)
    │
    └── Sub-Agent = 구현체 (How to do)
```

**장점:**
- 테스트 가능한 구조
- 유지보수 용이
- 명확한 책임 분리

### 5.4 조합 패턴 (Composition)

기존 Skill과 Sub-Agent를 조합하여 새로운 워크플로우를 생성합니다.

**적용 사례:**
- 새 콘텐츠 타입 추가
- 워크플로우 확장
- 도메인 간 기능 공유

**조합 예시:**

| 슬래시 커맨드 | 사용 스킬 | 사용 에이전트 |
|-------------|----------|--------------|
| `/podcast` | 딥리서치 + 콘텐츠제작 | Research, Analysis, Writing |
| `/proposal` | 문서작성 + 품질검증 | Analysis, Writing, Review |
| `/newsletter` | 딥리서치 + 콘텐츠제작 + 번역 | Research, Writing, Translation |

**원칙:**
- 컴포넌트는 단일 책임
- 조합으로 복잡성 해결
- N개 스킬 + M개 에이전트 = N×M 조합 가능

---

## 6. SDK 구현 가이드 (SDK Implementation Guide)

### 6.1 프로젝트 구조

```
hyperproof-agent-factory/
│
├── 📁 agents/                    # Sub-Agent 정의
│   ├── base_agent.py            # 공통 에이전트 인터페이스
│   ├── research_agent.py        # 웹검색/리서치
│   ├── analysis_agent.py        # 데이터 분석
│   ├── writing_agent.py         # 콘텐츠 작성
│   └── review_agent.py          # 품질 검토
│
├── 📁 skills/                    # Skill 정의
│   ├── base_skill.py            # 공통 스킬 인터페이스
│   ├── content_creation.py      # 콘텐츠 제작 오케스트레이션
│   ├── deep_research.py         # 딥리서치 로직
│   └── quality_check.py         # 품질 검증 로직
│
├── 📁 commands/                  # Slash Command
│   ├── podcast.py               # /podcast
│   ├── proposal.py              # /proposal
│   └── research.py              # /research
│
├── 📁 core/                      # 핵심 인프라
│   ├── orchestrator.py          # 실행 엔진
│   ├── registry.py              # 에이전트/스킬 등록
│   ├── context_manager.py       # 컨텍스트 관리
│   └── parallel_executor.py     # 병렬 처리
│
└── 📁 config/                    # 설정
    ├── agents.yaml              # 에이전트 설정
    ├── skills.yaml              # 스킬 조합 규칙
    └── workflows.yaml           # 워크플로우 정의
```

### 6.2 핵심 인터페이스

모든 컴포넌트는 다음 인터페이스를 구현합니다:

| 인터페이스 | 메서드 | 설명 |
|-----------|--------|------|
| `BaseAgent` | `execute(task, context)` | 독립 컨텍스트에서 작업 실행 |
| `BaseSkill` | `orchestrate(spec)` | 에이전트 조율 및 결과 통합 |
| `CommandHandler` | `handle(args)` | 사용자 입력 처리 및 스킬 호출 |
| `ParallelExecutor` | `run_parallel(tasks)` | 여러 에이전트 동시 실행 |

### 6.3 핵심 코드 예시

#### BaseAgent (재사용 가능)

```python
# agents/base_agent.py
from anthropic import Anthropic

class BaseAgent:
    def __init__(self, name: str, role: str, tools: list = None):
        self.name = name
        self.role = role
        self.tools = tools or []
        self.client = Anthropic()
    
    async def execute(self, task: str, context: dict = None):
        """독립 컨텍스트에서 실행"""
        response = await self.client.messages.create(
            model="claude-sonnet-4-20250514",
            system=f"You are {self.name}. Role: {self.role}",
            messages=[{"role": "user", "content": task}],
            tools=self.tools
        )
        return self.parse_response(response)
```

#### Skill Orchestrator

```python
# skills/content_creation.py
from core.parallel_executor import ParallelExecutor
from agents import ResearchAgent, AnalysisAgent, WritingAgent

class ContentCreationSkill:
    def __init__(self):
        self.agents = {
            "research": ResearchAgent(),
            "analysis": AnalysisAgent(),
            "writing": WritingAgent()
        }
        self.executor = ParallelExecutor()
    
    async def orchestrate(self, spec: dict):
        """병렬로 에이전트 실행 후 결과 통합"""
        
        # 1. 병렬 실행
        tasks = {
            "research": self.agents["research"].execute(spec["topic"]),
            "analysis": self.agents["analysis"].execute(spec["data"]),
            "writing": self.agents["writing"].execute(spec["outline"])
        }
        
        results = await self.executor.run_parallel(tasks)
        
        # 2. 결과 통합
        return self.merge_results(results)
```

### 6.4 YAML 기반 워크플로우 정의

새 워크플로우는 YAML 설정만으로 추가할 수 있습니다:

```yaml
# config/workflows.yaml

workflows:
  podcast:
    description: "Physical AI 팟캐스트 제작"
    skills:
      - deep_research
      - content_creation
    agents:
      - research_agent
      - analysis_agent
      - writing_agent
    parallel: true
    
  proposal:
    description: "정부 입찰 제안서"
    skills:
      - document_writing
      - quality_check
    agents:
      - analysis_agent
      - writing_agent
      - review_agent
    parallel: true

  # 새 워크플로우 추가 = YAML 몇 줄
  newsletter:
    description: "주간 AI 뉴스레터"
    skills:
      - deep_research
      - content_creation
      - localization
    agents:
      - research_agent
      - writing_agent
      - translation_agent
    parallel: true
```

### 6.5 동적 커맨드 빌더

```python
# core/command_builder.py
class CommandBuilder:
    def __init__(self, registry):
        self.registry = registry
    
    def create_command(self, name: str, skills: list, agents: list):
        """새 슬래시 커맨드 동적 생성"""
        
        async def command_handler(args: dict):
            # 스킬 조합
            skill_chain = [
                self.registry.get_skill(s) for s in skills
            ]
            
            # 실행
            result = args
            for skill in skill_chain:
                result = await skill.orchestrate(result)
            
            return result
        
        # 커맨드 등록
        self.registry.register_command(name, command_handler)
```

---

## 7. 확장 전략 (Scaling Strategy)

### 7.1 조합 기반 확장

컴포넌트의 선형 증가가 워크플로우의 기하급수적 확장으로 이어집니다:

| 시점 | Skill 수 | Agent 수 | 가능 워크플로우 |
|------|---------|---------|----------------|
| 1월 (초기) | 4개 | 5개 | ~12개 |
| 3월 | 6개 | 7개 | ~40개 |
| 6월 | 10개 | 10개 | ~100개+ |

### 7.2 컴포넌트 재사용

- **딥리서치 Skill**: podcast, proposal, research 등 여러 커맨드에서 공유
- **웹검색 Agent**: 다양한 Skill이 호출하는 공통 리소스
- **분석 Agent**: 데이터 처리가 필요한 모든 워크플로우에서 활용

### 7.3 유지보수 이점

| 측면 | 수동 설정 | SDK 기반 |
|------|----------|----------|
| 새 워크플로우 추가 | 파일 여러 개 수정 | YAML 몇 줄 |
| 에이전트 재사용 | 복사/붙여넣기 | 레지스트리에서 import |
| 병렬 처리 | 직접 구현 | `ParallelExecutor` 내장 |
| 에러 핸들링 | 각각 구현 | 중앙 집중 관리 |
| 버전 관리 | Git 충돌 빈번 | 설정 분리로 충돌 최소화 |
| 테스트 | 통합 테스트만 가능 | 유닛 테스트 가능 |

---

## 8. 로드맵 (Roadmap)

### Phase 1: 기반 구축 (1월)

- [ ] 핵심 아키텍처 설계 및 문서화
- [ ] 기본 에이전트 3개 구현 (Research, Analysis, Writing)
- [ ] 스킬 2개 구현 (콘텐츠 제작, 딥리서치)
- [ ] 슬래시 커맨드 2개 구현 (/podcast, /research)
- [ ] 팀 교육 및 온보딩

**산출물:**
- Physical AI 팟캐스트 1편
- 팀 교육 완료

### Phase 2: SDK 개발 (2월)

- [ ] Claude Code SDK 기반 프레임워크 프로토타입
- [ ] YAML 기반 워크플로우 설정 시스템
- [ ] 병렬 실행 엔진 구현
- [ ] 에이전트 5개로 확장
- [ ] 모니터링 대시보드 (선택)

**산출물:**
- 자동화된 콘텐츠 파이프라인
- 제안서 자동 생성

### Phase 3: 프로덕션 (3월)

- [ ] 전체 팀 프로덕션 사용
- [ ] Physical AI 팟캐스트 자동화 파이프라인 완성
- [ ] 제안서 자동 생성 시스템
- [ ] 오픈소스 공개 검토

**산출물:**
- Agent Factory 오픈소스?
- HypeProof 브랜딩

---

## Appendix: 용어집 (Glossary)

| 용어 | 정의 |
|------|------|
| **Slash Command** | `/command` 형식으로 호출하는 사용자 진입점 |
| **Skill** | 워크플로우를 정의하고 에이전트를 조율하는 오케스트레이터 |
| **Sub-Agent** | 독립 컨텍스트에서 전문 작업을 수행하는 실행자 |
| **Context Window** | 에이전트가 사용하는 메모리/작업 공간 |
| **Context Isolation** | 각 에이전트가 독립 컨텍스트를 사용하여 오염 방지 |
| **Parallel Execution** | 독립 작업을 동시에 실행하여 시간 단축 |
| **Orchestration** | 여러 에이전트의 작업을 조율하고 결과를 통합 |
| **Spec-Driven** | 명확한 스펙/계약을 기반으로 개발하는 방식 |
| **Agent Registry** | 에이전트를 등록하고 관리하는 중앙 저장소 |
| **Workflow** | 스킬과 에이전트 조합으로 정의된 작업 흐름 |

---

## 참고 자료 (References)

### 연구 및 문서

1. **Microsoft Azure Architecture Center** - AI Agent Orchestration Patterns
2. **LangChain Documentation** - Multi-Agent Systems
3. **Google ADK** - Developer's Guide to Multi-Agent Patterns
4. **AgentOrchestra** - Hierarchical Multi-Agent Framework (arXiv)
5. **Claude Code Documentation** - Skills, Subagents, Commands

### 관련 벤치마크

- LangChain Multi-Agent Efficiency Benchmark
- AgentOrchestra SimpleQA Benchmark (95.3% accuracy)
- VentureBeat Research on Multi-Agent Systems

---

**— End of Document —**

*HypeProof Lab © 2025*